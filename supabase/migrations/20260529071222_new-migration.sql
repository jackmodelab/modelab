-- Intentionally empty.
--
-- This placeholder was generated by `supabase migration new` and committed
-- before it was filled in. It is kept (rather than deleted) so the local
-- migration history stays in lockstep with any environment where this version
-- was already recorded as applied. Do not add schema changes here — create a
-- new timestamped migration instead.
